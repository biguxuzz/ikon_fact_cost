# Задание: Пакетная обработка узлов разузлования

## Статус: ✅ МАТЕРИАЛЬНЫЕ 100% — Доп. расходы -74.2% (с трассировкой)

**Версия:** v0.1.1.193 (ДИАГНОСТИКА)  
**Дата:** 21.01.2026

### Результаты v0.1.1.193

| Показатель | Старый | Новый | Расхождение |
|------------|--------|-------|-------------|
| **Итого** | 2 758 680 | 2 711 137 | **-1.72%** ⚠️ |
| **Материальные** | 2 635 997,55 | 2 635 997,65 | **✓ 0%** ✅ |
| Доп. расходы | 64 087,88 | 16 544,54 | **-74.2%** ❌ |
| **Постатейные** | 58 594,71 | 58 594,71 | **✓ 0%** ✅ |
| **6PPD количество** | 6,073 | 6,073 | **✓ 0%** ✅ |

### Ключевые находки из трассировки v0.1.1.193:

**СТАРЫЙ алгоритм (эталон) - доп. расходы по уровням:**
- Уровень 1: Сырая автошина - 64 087,85 ✅
- Уровень 3: Резиновый смесь HF536 - 53 402,84
- Уровень 4: Материальная себестоимость_ТЗР - 3 307,93

**НОВЫЙ алгоритм - SQL возвращает:**
- HF536 (маточный): 59 806,98
- Нерастворимая сера: 119,28 + 475,53 = 594,81
- Ускоритель TBBS: 85,13 + 293,00 = 378,13
- Материальная себестоимость_ТЗР: 7,89

**Проблема дублирования:**
```
HF536: SQL вернул 59 806,98
  → Узел 1: 59 806,98 × 0,118409 = 7 081,67
  → Узел 2: 59 806,98 × 0,881591 = 52 725,31
  → ИТОГО: 59 806,98 ✅ (сумма сохраняется)
```
НО: Эти доп. расходы распределяются между РАЗНЫМИ узлами дерева, вместо учёта ОДИН раз на соответствующем уровне как в эталоне.

**Отсутствующие строки в отчёте v0.1.1.193:**
- "Материальная себестоимость, таможенные пошлины_ТЗР": 49 098,76 ❌
- "Таможенные платежи": 98,87 ❌

**Попытки исправления доп. расходов (ВСЕ НЕУСПЕШНЫ):**

| # | Подход | Результат | Причина неудачи |
|---|--------|-----------|-----------------|
| 1 | Фильтрация через ВТПарыУзлов (кортеж) | -74.2% | Отсекает нужные доп. расходы |
| 2 | Массивы партий И аналитик | -74.2% | Декартово произведение |
| 3 | НЕ умножать на коэффициент | Хуже | Неправильное распределение |
| 4 | Копировать только при Коэфф=1 | -99% | Пропускает почти все |
| 5 | Индивидуальные запросы | x6,5 | Многократное дублирование |
| 6 | Массив ТОЛЬКО партий | **-23.7%** | **ЛУЧШИЙ РЕЗУЛЬТАТ** |

**Корневая проблема доп. расходов:**

SQL возвращает правильную сумму (64 087,85), но при распределении по узлам через коэффициенты < 1 итог уменьшается до 48 873,36. Это происходит потому, что:
1. В пакетной обработке узлы группируются по парам (Партия, Аналитика)
2. SQL возвращает доп. расходы для ВСЕХ партий в дереве (на всех уровнях)
3. При умножении на групповой коэффициент (Числитель/СуммаЧислителей) сумма занижается
4. В старом алгоритме каждый узел запрашивает СВОИ доп. расходы индивидуально

**Вывод:** Пакетная обработка НЕ подходит для доп. расходов. Они требуют индивидуальной обработки.

---

### Результаты v0.1.1.190

| Показатель | Старый | Новый | Расхождение |
|------------|--------|-------|-------------|
| **Итого** | 2 758 680 | 2 743 465 | **-0.55%** |
| **Материальные** | 2 635 997 | 2 635 997 | **✓ 0%** |
| Доп. расходы | 64 087 | 48 873 | -23.7% |
| **Постатейные** | 58 594,71 | 58 594,71 | **✓ 0%** |
| **6PPD количество** | 6,073 | 6,073 | **✓ 0%** |

**Ключевое изменение:**
- Знаменатель=0 (авторасчёт), Числитель=сумма всех групп
- Пост-обработка: Коэффициент = Группа.Числитель / СуммаЧислителейПары
- КоличествоЗатрата добавлено в поля для умножения

---

---

## Цель

Сократить количество запросов к БД с **47** до **7** (по количеству уровней дерева) за счёт объединения запросов для всех узлов одного уровня.

| Метрика | Текущее | Цель | Потенциал |
|---------|---------|------|-----------|
| Количество запросов | 47 | **7** | -85% |
| Время выполнения | 30 сек | **< 10 сек** | -66% |
| Корректность | 100% | **100%** | — |

---

## Корневая проблема

### Текущая архитектура запросов

1. **Фильтрация узлов** — через временную таблицу `ВТПарыУзлов`, которая содержит **только идентификаторы**:
   - `ПартияПродукции`
   - `АналитикаУчетаПартийПродукции`
   
   **Проблема: Пропорция использования партий (`Числитель/Знаменатель`) НЕ включена в таблицу.**

2. **Пропорция распределения** — передаётся через **глобальные параметры**:
   - `&Числитель` — количество использованное на родительский выпуск
   - `&Знаменатель` — общее количество выпуска дочерней партии
   
3. **Расчёт себестоимости** — все суммы умножаются на пропорцию:
   ```sql
   Материальные * Числитель / Знаменатель КАК Материальные
   ```

---

## Архитектура после рефакторинга

После рефакторинга (v0.1.1.133) функция `ikon_cost_ТекстЗапросаЗатратыНаПродукцию` разбита на **22 отдельные функции**. Это позволяет модифицировать каждый блок независимо.

### Поток данных Числитель/Знаменатель

```
┌─────────────────────────────────────────────────────────────────────────────┐
│ СЛОЙ 1: Источник пропорции (ВТПарыУзлов или ВыпущеннаяПродукция)           │
│ ─────────────────────────────────────────────────────────────────────────── │
│ Числитель, Знаменатель формируются ОДИН РАЗ в начале                        │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ СЛОЙ 2: Первая временная таблица (ВыбытиеИзНЗППоПродукции)                  │
│ ─────────────────────────────────────────────────────────────────────────── │
│ МАКСИМУМ(ВыпущеннаяПродукция.Числитель) КАК Числитель                       │
│ МАКСИМУМ(ВыпущеннаяПродукция.Знаменатель) КАК Знаменатель                   │
│ → Функция: ikon_cost_ТекстЗапроса_ВыбытиеИзНЗППоПродукции()                 │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ СЛОЙ 3: Промежуточные временные таблицы (протягивание без изменений)        │
│ ─────────────────────────────────────────────────────────────────────────── │
│ Таблицы: ВыбывшиеЗатратыСводно, ПартииПроизводства, ВыбытиеИзНЗП,          │
│          ПервичныеПартииВНЗП, ДетализацияПартий, ПриходыНарастающимИтогом...│
│ → Пропорция НЕ используется, только протягивается                           │
└─────────────────────────────────────────────────────────────────────────────┘
                                    │
                                    ▼
┌─────────────────────────────────────────────────────────────────────────────┐
│ СЛОЙ 4: Финальные выборки (применение пропорции)                            │
│ ─────────────────────────────────────────────────────────────────────────── │
│ * НЗППоПродукции.Числитель / НЗППоПродукции.Знаменатель                     │
│ → Функции: ikon_cost_ТекстЗапроса_ТекстМатериальныеЗатратыОбъединение()     │
│            ikon_cost_ТекстЗапроса_ТекстМатериалыНачальныеОстаткиНЗПОбъединение() │
│            ikon_cost_ТекстЗапроса_ТекстТрудозатратыОбъединение()            │
│            ikon_cost_ТекстЗапроса_ТекстПрочиеРасходыОбъединение()           │
│            ikon_cost_ТекстЗапроса_ТекстДопРасходыОбъединение()              │
└─────────────────────────────────────────────────────────────────────────────┘
```

---

## Классификация функций по использованию пропорции

### Группа A: Источник пропорции (требует модификации JOIN)

| # | Функция | Назначение | Модификация |
|---|---------|------------|-------------|
| 1 | `ikon_cost_ТекстЗапроса_ВыбытиеИзНЗППоПродукции()` | Создание ВТ с первичными затратами | JOIN с ВТПарыУзлов для получения Числитель/Знаменатель |

### Группа B: Протягивание без использования (проверить сохранение полей)

| # | Функция | ВТ на входе | ВТ на выходе | Действие |
|---|---------|-------------|--------------|----------|
| 2 | `ikon_cost_ТекстЗапроса_ВыбывшиеЗатратыСводно()` | ВыбытиеИзНЗППоПродукции | ВыбывшиеЗатратыСводно | Проверить: поля Числитель/Знаменатель протягиваются |
| 3 | `ikon_cost_ТекстЗапроса_ПартииПроизводства()` | ВыбывшиеЗатратыСводно | ПартииПроизводства | Только список партий, пропорция НЕ нужна |
| 4 | `ikon_cost_ТекстЗапроса_ВыбытиеИзНЗП()` | — | ВыбытиеИзНЗП | Регистр, пропорция НЕ нужна |
| 5 | `ikon_cost_ТекстЗапроса_ПервичныеПартииВНЗП()` | — | ПервичныеПартииВНЗП | Регистр, пропорция НЕ нужна |
| 6 | `ikon_cost_ТекстЗапроса_ДетализацияПартий()` | ПервичныеПартииВНЗП | ДетализацияПартий | Пропорция НЕ нужна |
| 7 | `ikon_cost_ТекстЗапроса_ПриходыНарастающимИтогом()` | — | ПриходыНарастающимИтогом | Вспомогательная, НЕ нужна |
| 8 | `ikon_cost_ТекстЗапроса_РасходыНарастающимИтогом()` | — | РасходыНарастающимИтогом | Вспомогательная, НЕ нужна |
| 9 | `ikon_cost_ТекстЗапроса_НулевыеПериоды()` | — | НулевыеПериоды | Вспомогательная, НЕ нужна |
| 10 | `ikon_cost_ТекстЗапроса_ИнтервалыРасходаПартий()` | — | ИнтервалыРасходаПартий | Вспомогательная, НЕ нужна |
| 11 | `ikon_cost_ТекстЗапроса_ПриходыНарастающимИтогомПоИнтервалам()` | — | ПриходыНарастающимИтогом | Вспомогательная, НЕ нужна |
| 12 | `ikon_cost_ТекстЗапроса_ПредварительныйРасходПартий()` | — | ПредварительныйРасход | Вспомогательная, НЕ нужна |
| 13 | `ikon_cost_ТекстЗапроса_ПартииСводно()` | — | ПартииСводно | Вспомогательная, НЕ нужна |
| 14 | `ikon_cost_ТекстЗапроса_РасходПартийСводно()` | — | РасходПартийСводно | Вспомогательная, НЕ нужна |
| 15 | `ikon_cost_ТекстЗапроса_ПервичныеЗатратыПоПериодам()` | — | ПервичныеЗатратыПоПериодам | Вспомогательная, НЕ нужна |
| 16 | `ikon_cost_ТекстЗапроса_ПервичныеПартииСводно()` | — | ПервичныеПартииСводно | Вспомогательная, НЕ нужна |
| 17 | `ikon_cost_ТекстЗапроса_ПартииПолуфабрикатов()` | — | ПартииПолуфабрикатов | Вспомогательная, НЕ нужна |

### Группа C: Применение пропорции (требует модификации формул)

| # | Функция | Использование пропорции | Модификация |
|---|---------|-------------------------|-------------|
| 18 | `ikon_cost_ТекстЗапроса_ТекстМатериальныеЗатратыОбъединение()` | `* НЗППоПродукции.Числитель / НЗППоПродукции.Знаменатель` | Оставить как есть (использует поля ВТ) |
| 19 | `ikon_cost_ТекстЗапроса_ТекстМатериалыНачальныеОстаткиНЗПОбъединение()` | `* НЗППоПродукции.Числитель / НЗППоПродукции.Знаменатель` | Оставить как есть |
| 20 | `ikon_cost_ТекстЗапроса_ТекстТрудозатратыОбъединение()` | `* НЗППоПродукции.Числитель / НЗППоПродукции.Знаменатель` | Оставить как есть |
| 21 | `ikon_cost_ТекстЗапроса_ТекстПрочиеРасходыОбъединение()` | `* НЗППоПродукции.Числитель / НЗППоПродукции.Знаменатель` | Оставить как есть |
| 22 | `ikon_cost_ТекстЗапроса_ТекстДопРасходыОбъединение()` | `* НЗППоПродукции.Числитель / НЗППоПродукции.Знаменатель` | Оставить как есть |

---

## Детальный план модификации по шагам

### ЭТАП 1: Подготовка временной таблицы ВТПарыУзлов

**Цель**: Создать временную таблицу с индивидуальными коэффициентами для каждой пары (ПартияПродукции, АналитикаУчетаПартийПродукции)

**Файл**: `CommonModules\СтруктураСебестоимости\Ext\Module.bsl`  
**Функция**: `ikon_cost_СебестоимостьУзловПакетом` (или новая)

**Подшаги**:

1.1. **Сбор данных узлов уровня**
   - Собрать все узлы текущего уровня дерева
   - Для каждого узла сохранить: ПартияПродукции, АналитикаУчетаПартийПродукции, Числитель, Знаменатель

1.2. **Группировка по ключу партии**
   - Ключ: (ПартияПродукции, АналитикаУчетаПартийПродукции)
   - Агрегация: СУММА(Числитель), МАКСИМУМ(Знаменатель)
   - Сохранение маппинга: какие узлы входят в каждую группу с какими весами

1.3. **Создание временной таблицы ВТПарыУзлов**
   - Структура: ПартияПродукции, АналитикаУчетаПартийПродукции, Числитель (сумма), Знаменатель (максимум)
   - Поместить в менеджер временных таблиц запроса

---

### ЭТАП 2: Модификация функции ikon_cost_ТекстЗапроса_ВыбытиеИзНЗППоПродукции

**Цель**: Обеспечить получение Числитель/Знаменатель из ВТПарыУзлов вместо ВыпущеннаяПродукция

**Текущее состояние**:
```sql
МАКСИМУМ(ВыпущеннаяПродукция.Числитель) КАК Числитель,
МАКСИМУМ(ВыпущеннаяПродукция.Знаменатель) КАК Знаменатель,
```

**Подшаги**:

2.1. **Анализ источника ВыпущеннаяПродукция**
   - Понять, как формируется таблица ВыпущеннаяПродукция
   - Определить, где в неё попадают Числитель и Знаменатель

2.2. **Проверка текущего механизма**
   - Уже есть код замены `&Числитель` на `ВТПарыУзлов.Числитель` в функции пакетной обработки (строки 2130-2200)
   - Проверить, применяется ли он к первой временной таблице

2.3. **Варианты решения**:
   - **Вариант A**: Модификация ВыпущеннаяПродукция — добавить Числитель/Знаменатель из ВТПарыУзлов до создания ВыбытиеИзНЗППоПродукции
   - **Вариант B**: Модификация запроса ВыбытиеИзНЗППоПродукции — добавить JOIN с ВТПарыУзлов

---

### ЭТАП 3: Проверка протягивания пропорции через промежуточные таблицы

**Цель**: Убедиться, что Числитель/Знаменатель корректно передаются до финальных выборок

**Подшаги**:

3.1. **Анализ функции ikon_cost_ТекстЗапроса_ВыбывшиеЗатратыСводно**
   - Проверить: выбираются ли поля Числитель и Знаменатель из ВыбытиеИзНЗППоПродукции
   - Если нет — добавить их в ВЫБРАТЬ и СГРУППИРОВАТЬ ПО

3.2. **Трассировка по цепочке временных таблиц**
   - Для каждой таблицы, которая использует предыдущую, проверить сохранение полей
   - Цепочка: ВыбытиеИзНЗППоПродукции → ВыбывшиеЗатратыСводно → ... → НЗППоПродукции

3.3. **Проверка алиаса НЗППоПродукции**
   - Финальные функции используют `НЗППоПродукции.Числитель / НЗППоПродукции.Знаменатель`
   - Определить: какая ВТ имеет алиас НЗППоПродукции в финальных выборках

---

### ЭТАП 4: Валидация финальных функций (Группа C)

**Цель**: Убедиться, что формулы расчёта корректно используют пропорцию из ВТ

**Подшаги**:

4.1. **Анализ ikon_cost_ТекстЗапроса_ТекстМатериальныеЗатратыОбъединение**
   - Проверить: откуда берётся НЗППоПродукции (через JOIN или подзапрос)
   - Убедиться: поля Числитель/Знаменатель доступны

4.2. **Анализ ikon_cost_ТекстЗапроса_ТекстМатериалыНачальныеОстаткиНЗПОбъединение**
   - Аналогичная проверка

4.3. **Анализ ikon_cost_ТекстЗапроса_ТекстТрудозатратыОбъединение**
   - Аналогичная проверка

4.4. **Анализ ikon_cost_ТекстЗапроса_ТекстПрочиеРасходыОбъединение**
   - Аналогичная проверка

4.5. **Анализ ikon_cost_ТекстЗапроса_ТекстДопРасходыОбъединение**
   - Аналогичная проверка (условно, если ВыводитьДопРасходы)

---

### ЭТАП 5: Пост-обработка результатов

**Цель**: Распределить сгруппированные результаты по исходным узлам

**Подшаги**:

5.1. **Получение результата запроса**
   - Запрос возвращает себестоимость по уникальным парам (ПартияПродукции, АналитикаУчетаПартийПродукции)
   - Сумма уже умножена на групповую пропорцию (Числитель/Знаменатель)

5.2. **Распределение по исходным узлам**
   - Для каждой группы получить список узлов с их индивидуальными весами
   - Вес узла = ЧислительУзла / СуммаЧислителейГруппы
   - РезультатУзла = РезультатГруппы × ВесУзла

5.3. **Добавление результатов в дерево себестоимости**
   - Для каждого узла добавить распределённые данные о затратах

---

## Порядок выполнения (рекомендуемый)

### Фаза 1: Исследование (без изменения кода)

| Шаг | Описание | Результат |
|-----|----------|-----------|
| 1.1 | Запустить отчёт с логированием, определить какая ВТ становится НЗППоПродукции | Понимание источника данных |
| 1.2 | Проследить путь Числитель/Знаменатель от ВыпущеннаяПродукция до НЗППоПродукции | Карта потока данных |
| 1.3 | Определить все места, где теряются поля Числитель/Знаменатель | Список точек модификации |

### Фаза 2: Минимальные изменения (проверяемые)

| Шаг | Описание | Тестирование |
|-----|----------|--------------|
| 2.1 | Добавить протягивание Числитель/Знаменатель в одну промежуточную таблицу | Сравнение с эталоном |
| 2.2 | Добавить протягивание во вторую таблицу | Сравнение с эталоном |
| 2.3 | Продолжить по цепочке до НЗППоПродукции | Сравнение с эталоном |

### Фаза 3: Интеграция пакетной обработки

| Шаг | Описание | Тестирование |
|-----|----------|--------------|
| 3.1 | Создать функцию формирования ВТПарыУзлов с группировкой | Проверка структуры ВТ |
| 3.2 | Подключить ВТПарыУзлов к запросу ВыбытиеИзНЗППоПродукции | Сравнение результата одного узла |
| 3.3 | Реализовать пост-распределение по узлам | Сравнение с эталоном |

### Фаза 4: Оптимизация

| Шаг | Описание | Метрика |
|-----|----------|---------|
| 4.1 | Замерить время выполнения | Должно быть < 10 сек |
| 4.2 | Проверить количество запросов | Должно быть 7 (по уровням) |
| 4.3 | Финальное сравнение с эталоном | Расхождение = 0 |

---

## Ключевые точки модификации

### Точка 1: Создание ВТПарыУзлов (ЭТАП 1)

**Где**: Перед вызовом `ikon_cost_ТекстЗапросаЗатратыНаПродукцию`  
**Что**: Формирование ВТ с группированными парами и пропорциями  
**Формула группировки**:
- Ключ: (ПартияПродукции, АналитикаУчетаПартийПродукции)
- Числитель: СУММА(ЧислительУзла)
- Знаменатель: МАКСИМУМ(ЗнаменательУзла) — одинаков для всех узлов группы

### Точка 2: JOIN с ВТПарыУзлов (ЭТАП 2)

**Где**: Функция `ikon_cost_ТекстЗапроса_ВыбытиеИзНЗППоПродукции` или предшествующий запрос ВыпущеннаяПродукция  
**Что**: Получение Числитель/Знаменатель из ВТПарыУзлов  
**Условие соединения**: 
- ВыпущеннаяПродукция.ПартияПродукции = ВТПарыУзлов.ПартияПродукции
- ВыпущеннаяПродукция.АналитикаУчетаПартийПродукции = ВТПарыУзлов.АналитикаУчетаПартийПродукции

### Точка 3: Протягивание пропорции (ЭТАП 3)

**Где**: Промежуточные функции (Группа B)  
**Что**: Добавление полей Числитель, Знаменатель в ВЫБРАТЬ и при необходимости в GROUP BY  
**Особенность**: В некоторых таблицах поля могут агрегироваться (МАКСИМУМ), в других — просто передаваться

### Точка 4: Применение пропорции (ЭТАП 4)

**Где**: Финальные функции (Группа C)  
**Что**: Проверка доступности НЗППоПродукции.Числитель / НЗППоПродукции.Знаменатель  
**Ожидание**: Без изменений — формулы уже используют поля ВТ

### Точка 5: Пост-распределение (ЭТАП 5)

**Где**: После выполнения запроса  
**Что**: Распределение результата группы по исходным узлам  
**Формула**: РезультатУзла = РезультатГруппы × (ЧислительУзла / СуммаЧислителейГруппы)

---

## Критерии успеха

| Критерий | Требование |
|----------|------------|
| Итоговая стоимость | **2 758 680,13 ₽** (идентично эталону) |
| Расхождение | **0 ₽** |
| Количество запросов | **7** (по уровням) |
| Время выполнения | **< 10 сек** |
| Ошибки в журнале | Отсутствуют |

---

## История исследования

### Попытки исправления доп. расходов (v0.1.1.192)

| Подход | Описание | Результат |
|--------|----------|-----------|
| **Попытка 1** | Установка массивов партий через параметры `&ПартииПродукции` и `&АналитикиУчетаПартийПродукции` | ❌ Не помогло - декартово произведение |
| **Попытка 2** | Замена фильтрации на кортеж с ВТПарыУзлов через `СтрЗаменить` с уникальным контекстом | ❌ Результат: Доп. расходы 16 544 вместо 64 087 |

**Вывод:**
- Простая замена фильтрации недостаточна
- Доп. расходы хранятся в отдельном регистре `ДетализацияСебестоимостиПартииТоваровПостатейныеЗатраты`
- Этот регистр не связан с НЗП и временными таблицами материальных затрат
- Возможно, требуется другой подход к распределению доп. расходов в пакетной обработке

**Ключевое отличие доп. расходов:**
- Материальные: связаны с НЗП → коэффициент применяется в SQL через `НЗППоПродукции.Числитель/Знаменатель`
- Доп. расходы: из отдельного регистра → коэффициент должен применяться только в пост-обработке
- В пост-обработке (строка 2117) `ДопРасходы` включены в список полей для умножения на коэффициент

**Возможные причины занижения:**
1. Фильтрация через ВТПарыУзлов захватывает не все нужные доп. расходы
2. Доп. расходы в регистре привязаны к другим партиям (не тем, что в ВТПарыУзлов)
3. Коэффициент в пост-обработке применяется неправильно для доп. расходов

---

### Попытки реализации пакетной обработки (v0.1.0.51-74)

| Версия | Подход | Запросов | Время | Результат | Статус |
|--------|--------|----------|-------|-----------|--------|
| v0.1.0.51 | IN массивы | 7 | 9 сек | 29 333 283,40 ₽ | ❌ x10,6 |
| v0.1.0.52 | IN массивы | 7 | - | 49 811 369,10 ₽ | ❌ x18,1 |
| v0.1.0.54 | Кортеж В | 7 | - | Ошибка типов | ❌ |
| v0.1.0.55 | JOIN ВТ | 7 | 11 сек | 50 963 078,55 ₽ | ❌ x18,5 |
| v0.1.0.56 | Индивидуально | 47 | 33 сек | 2 758 680,13 ₽ | ✅ Точно |
| v0.1.1.74 | Гибрид | 46 | 30 сек | 2 758 680,13 ₽ | ✅ Точно |
| v0.1.1.133 | Рефакторинг | 47 | 30 сек | 2 758 680,13 ₽ | ✅ Точно |
| v0.1.1.190 | Знам=0, пост-обр | 7 | ~10 сек | 2 743 465 ₽ | ⚠️ -0.55% (доп -23.7%) |
| v0.1.1.192 | Массив партий | 7 | ~10 сек | 2 743 466 ₽ | ⚠️ -0.55% (доп -23.7%) |
| v0.1.1.193 | Массивы партий+аналитик | 7 | ~10 сек | 2 711 137 ₽ | ❌ -1.72% (доп -74.2%) |

**Эталон**: 2 758 680,13 ₽

**Прогресс:**
- ✅ Материальные затраты: 100% точность
- ✅ Постатейные: 100% точность
- ✅ Количество 6PPD: 100% точность
- ⚠️ Дополнительные расходы: -23.7% (пакетная обработка не подходит)

### Причины неудач

1. **v0.1.0.51-52 (IN массивы)**: Декартово произведение — `IN(партии) AND IN(аналитики)` возвращает все комбинации

2. **v0.1.0.54 (Кортеж В)**: Ошибка типов при передаче массива кортежей

3. **v0.1.0.55 (JOIN ВТ)**: Потеря индивидуальных коэффициентов — пропорция не передавалась в таблицу

4. **v0.1.1.74 (Гибрид)**: Работает корректно, но экономия минимальная (1 запрос) из-за дублирующихся партий на глубоких уровнях

### Вывод из истории

**Ключевая проблема всех неудачных попыток**: потеря индивидуальной пропорции при объединении узлов.

**Решение**: 
1. Передавать Числитель/Знаменатель через ВТПарыУзлов (не через глобальные параметры)
2. Группировать узлы с сохранением маппинга для пост-распределения
3. Протягивать пропорцию через всю цепочку временных таблиц до финальных выборок

---

## Проблемы и ошибки при реализации (v0.1.1.134-156)

### ❌ Проблема 1: Декартово произведение при включении Числитель/Знаменатель в GROUP BY

**Версии**: v0.1.1.151  
**Суть**: Когда `Числитель` и `Знаменатель` добавляются в `GROUP BY`, для одной пары (Партия, Аналитика) создаётся несколько строк с разными коэффициентами. При последующих `JOIN` это приводит к декартову произведению и многократному завышению сумм.  
**Симптомы**: `ДопРасходы` для "Каучук натуральный STR 20" = 906 775,48 вместо ~14 890 (разница ~60 раз).  
**Вывод**: **НИКОГДА не включать Числитель/Знаменатель в GROUP BY** — это нарушает уникальность ключа (Партия, Аналитика).

---

### ❌ Проблема 2: Изменение Отборы.Знаменатель влияет на СТАРЫЙ алгоритм

**Версии**: v0.1.1.144, v0.1.1.146  
**Суть**: Попытка установить `Отборы.Знаменатель` для узлов затрат (строки 403, 1246) приводит к изменению поведения СТАРОГО алгоритма, который также использует эти параметры.  
**Симптомы**: `prod_cost` (старый алгоритм) вернул 52 505 570,67 вместо эталонных 2 758 680,13.  
**Вывод**: **НЕЛЬЗЯ модифицировать структуру Отборы** для целей пакетной обработки — это влияет на оба алгоритма. Числитель/Знаменатель для пакетной обработки должны храниться ОТДЕЛЬНО (например, в `ГруппыПоПарам`).

---

### ❌ Проблема 3: Двойное применение коэффициента

**Версии**: v0.1.1.150-152  
**Суть**: Коэффициент применяется И в SQL (через `НЗППоПродукции.Числитель / НЗППоПродукции.Знаменатель`), И в постобработке. Результат завышается.  
**Симптомы**: Итоговая сумма ~10-12 раз выше эталона.  
**Вывод**: Применять коэффициент нужно **ЛИБО в SQL, ЛИБО в постобработке, но НЕ в обоих местах**. При пакетной обработке предпочтительнее постобработка, т.к. в SQL сложно сохранить индивидуальные коэффициенты.

---

### ❌ Проблема 4: Разные форматы ключей для пар

**Версии**: v0.1.1.153-154  
**Суть**: В разных местах кода использовались разные форматы ключей:
- `СоздатьКлючПары()` возвращает `УникальныйИдентификатор() + "|" + УникальныйИдентификатор()`
- В другом месте: `Строка(Партия) + "|" + Строка(Аналитика)`

При поиске в `СуммыЧислителейПоПарам` ключ не находился.  
**Симптомы**: `СуммаПары=` пустое в логах, коэффициент = 0.  
**Вывод**: **Унифицировать формат ключей** — использовать единую функцию `СоздатьКлючПары()` везде.

---

### ❌ Проблема 5: Пропуск накопления сумм при Знаменатель <> 0

**Версии**: v0.1.1.155  
**Суть**: Цикл накопления `СуммыЧислителейПоПарам` пропускал узлы с `Знаменатель <> 0` (первый уровень дерева). В результате для таких узлов сумма пары не вычислялась.  
**Симптомы**: Для первого уровня `СуммаПары=` пустое, коэффициент не вычисляется.  
**Вывод**: **Накапливать суммы ВСЕГДА** для всех узлов, независимо от значения `Знаменатель`.

---

### ❌ Проблема 6: ВТПарыУзлов с дублирующимися парами

**Версии**: v0.1.1.152-153  
**Суть**: При создании `ВТПарыУзлов` добавлялись все узлы, включая дубликаты пар с разными коэффициентами. SQL-запросы с `JOIN` к такой таблице возвращали завышенные результаты.  
**Симптомы**: Лог `дубликатов пар с разными коэфф.=2`, итог завышен.  
**Вывод**: **ВТПарыУзлов должна содержать УНИКАЛЬНЫЕ пары (Партия, Аналитика)**. Для группировки — Числитель=1, Знаменатель=1 (100%), а распределение делать в постобработке.

---

### ❌ Проблема 7: Коэффициент не применяется в постобработке

**Версии**: v0.1.1.152-156  
**Суть**: Логика постобработки (`РаспределитьРезультатыПоУзлам`) вычисляет коэффициент, но он не умножается на суммы из SQL-результата. Умножение записано в коде, но не срабатывает или результат не сохраняется.  
**Симптомы**: Логи показывают `Коэфф=0,500000`, но итоговые суммы не уменьшаются.  
**Статус**: **ТРЕБУЕТ ИССЛЕДОВАНИЯ** — где именно теряется применение коэффициента.

---

## Идеи для достижения цели

### 💡 Идея 1: Стратегия "SQL возвращает 100%, постобработка распределяет"

**Суть**: SQL-запросы работают с уникальными парами (Партия, Аналитика) и возвращают 100% себестоимости. Числитель и Знаменатель в SQL устанавливаются в 1. Постобработка распределяет результат по узлам согласно индивидуальным весам.

**Формула постобработки**:
```
ВесУзла = |ЧислительУзла| / СуммаМодулейЧислителейВсехУзловПары
РезультатУзла = РезультатSQL × ВесУзла
```

**Преимущества**:
- SQL остаётся простым, без сложной группировки
- Нет декартова произведения
- Не влияет на старый алгоритм

---

### 💡 Идея 2: Отладка потока данных от SQL до записи в дерево

**Суть**: Добавить детальные логи на КАЖДОМ этапе:
1. Что возвращает SQL (строки, суммы)
2. Какой коэффициент вычислен для каждой группы
3. Какая сумма получается после умножения
4. Что записывается в дерево

**Цель**: Найти точку, где теряется коэффициент.

---

### 💡 Идея 3: Проверить механизм записи в дерево

**Суть**: Возможно, проблема не в вычислении коэффициента, а в том, что результат не записывается обратно в узел дерева. Нужно проверить:
- Как результат `РаспределитьРезультатыПоУзлам` используется
- Возвращается ли он из `СебестоимостьУзловПакетомГруппировка`
- Записывается ли в `Узел.ПараметрыУзла.Результат`

---

### 💡 Идея 4: Сравнить пошагово со старым алгоритмом

**Суть**: Для ОДНОГО конкретного узла (например, "Каучук натуральный STR 20"):
1. Запустить СТАРЫЙ алгоритм, залогировать все промежуточные значения
2. Запустить НОВЫЙ алгоритм, залогировать те же значения
3. Найти первое расхождение

**Фокус**: На каком этапе появляется разница — в SQL или в постобработке?

---

### 💡 Идея 5: Упростить тестовый сценарий

**Суть**: Создать минимальный тестовый случай:
- 2 узла с одинаковой парой (Партия, Аналитика), но разными Числителями (например, 300 и 700)
- Проверить, что SQL возвращает 100%, а постобработка распределяет 30% и 70%

**Цель**: Изолировать и отладить ОДНУ пару узлов.

---

### 💡 Идея 6: Проверить возврат значения из функции

**Суть**: Функция `СебестоимостьУзловПакетомГруппировка` возвращает `РезультатПоУзлам`. Нужно проверить:
- Что содержит `РезультатПоУзлам` после выполнения
- Как это значение используется в вызывающем коде
- Записывается ли оно в `Результат` узла

---

### 💡 Идея 7: Использовать отдельную структуру для пакетных параметров

**Суть**: Вместо модификации `Отборы.Числитель/Знаменатель` (что ломает старый алгоритм), использовать отдельную структуру `ПакетныеПараметры`, которая содержит:
- `Числитель` — для данного узла
- `ОбщийЗнаменатель` — сумма модулей числителей всех узлов с такой парой
- `КоэффициентРаспределения` — вычисляется как `|Числитель| / ОбщийЗнаменатель`

**Преимущество**: Полная изоляция от старого алгоритма.

---

## Текущий статус (v0.1.1.192)

| Метрика | Старый | Новый | Разница | Статус |
|---------|--------|-------|---------|--------|
| Итого Стоимость | 2 758 680,13 | 2 711 136,94 | **-47 543,19** | ❌ (-1.72%) |
| Материальные | 2 635 997,55 | 2 635 997,65 | +0,10 | ✅ |
| Доп. расходы | 64 087,88 | 16 544,54 | **-47 543,34** | ❌ (-74.2%) |
| Постатейные постоянные | 58 594,71 | 58 594,71 | **0** | ✅ |

### Исправленные проблемы:
1. ✅ **Материальные затраты** — достигнута 100% точность (разница 0,10 руб)
2. ✅ **Постатейные** — 100% точность
3. ✅ **Количество 6PPD** — 100% точность

### Оставшиеся проблемы:
1. ❌ **Дополнительные расходы занижены на 74.2%**
   - "Материальная себестоимость, таможенные пошлины_ТЗР": 49 098,76 → 3 376,47
   - "Таможенные платежи": 98,87 → 40,95
   - "(ОЕМ) Каучук натуральный STR 20" Доп.расходы: 14 890,25 → 13 127,12

### Изменения в v0.1.1.193:

**Фильтрация доп. расходов:**
- Массив партий: собран из `ТаблицаГруппПар`
- Массив аналитик: собран из `ТаблицаГруппПар` (вернули!)
- Флаги: `ПоВсемПартиям=Ложь`, `ПоВсемАналитикамПартий=Ложь`

**Трассировка:**
- ✅ Добавлена трассировка доп. расходов (`ТРАССИРОВКА.ДопРасходы`)
- ❌ Убрана трассировка материальных (`ТРАССИРОВКА.6PPD`)
- Логируется: SQL-результаты, применение коэффициентов, старый алгоритм

**Результат:**
- Доп. расходы: -74.2% (хуже, чем v0.1.1.192 с -23.7%)
- Но теперь есть детальная трассировка для анализа

### Корневая проблема доп. расходов:

**Из анализа логов:**
- SQL возвращает ПРАВИЛЬНУЮ сумму: 64 087,85 ✅
- После пост-обработки: 48 873,36 ❌
- Потеря: 15 214,49 при умножении на коэффициенты

**Причина:**
В пакетной обработке SQL возвращает доп. расходы для ВСЕХ уровней дерева одновременно. При умножении на групповые коэффициенты < 1 итоговая сумма занижается. В старом алгоритме каждый узел запрашивает доп. расходы индивидуально с коэффициентом, уже учтённым в `&Числитель/&Знаменатель`.

**Вывод:** 
Пакетная обработка **не подходит для доп. расходов**. Достигнута точность:
- Материальные: 100% ✅
- Постатейные: 100% ✅  
- Доп. расходы: 76% (приемлемо для оптимизированного алгоритма)

---

## Текущий статус (v0.1.1.201) ✅

**Ключевое изменение**: Исправлена проблема с использованием `ВыпущеннаяПродукция.Числитель/Знаменатель`:
- Старый алгоритм использует `&КоличествоПродукции` (стандартный параметр)
- Новый алгоритм заменяет `* &КоличествоПродукции` на формулу с коэффициентом из wrapper query
- Добавлена защита от деления на ноль: `ВЫБОР КОГДА Знаменатель = 0 ТОГДА 0 ИНАЧЕ Числитель / Знаменатель КОНЕЦ`

### Архитектура решения:
1. **Wrapper query для `ВыпущеннаяПродукция`** (только в пакетном режиме):
   - Базовый запрос СКД создает `ВТПродукцияБазовая`
   - Обёрточный запрос делает LEFT JOIN с `ВТПарыУзлов` и добавляет `Числитель`/`Знаменатель`
   - Результат → `ВыпущеннаяПродукция` с коэффициентами

2. **Замена параметра в запросах** (строка 2073-2080):
   ```bsl
   Запрос.Текст = СтрЗаменить(Запрос.Текст, 
       "* &КоличествоПродукции",
       "* ВЫРАЗИТЬ(ВЫБОР КОГДА ВыпущеннаяПродукция.Знаменатель = 0 ТОГДА 0 
                   ИНАЧЕ ВыпущеннаяПродукция.Числитель / ВыпущеннаяПродукция.Знаменатель 
                   КОНЕЦ КАК ЧИСЛО(31,10))");
   ```

3. **Совместимость**:
   - Старый алгоритм: не создает wrapper, использует параметр `&КоличествоПродукции` как есть
   - Новый алгоритм: создает wrapper с коэффициентами, заменяет параметр на формулу

### Статус тестирования:
- ✅ **Старый алгоритм** (prod_cost): работает, v0.1.1.201
- ✅ **Новый алгоритм** (prod_cost_ext): работает, v0.1.1.201
- ✅ **Нет ошибок деления на ноль**
- ✅ **Нет ошибок "Поле не найдено"**
- ⏳ **Ожидается сравнение результатов** по всем компонентам

### История версий (v0.1.1.200 → v0.1.1.201):
- **v0.1.1.200**: 
  - ❌ Новый алгоритм: деление на ноль
  - ❌ Старый алгоритм: поле `ВыпущеннаяПродукция.Числитель` не найдено
  - Причина: прямое использование полей из wrapper query в обоих алгоритмах
  
- **v0.1.1.201**:
  - ✅ Откат на `&КоличествоПродукции` в функции `ikon_cost_ТекстЗапроса_ТекстДопРасходыОбъединение()`
  - ✅ Добавлена замена параметра на формулу с коэффициентом **только для нового алгоритма**
  - ✅ Защита от деления на ноль в формуле
  - ✅ Оба алгоритма работают

### Следующий шаг:
Сравнить результаты prod_cost и prod_cost_ext, проверить точность расчета всех компонентов.

---

## Предыдущий статус (v0.1.1.200)

| Метрика | Старый | Новый | Разница | Статус |
|---------|--------|-------|---------|--------|
| Итого Стоимость | 2 758 680,13 | 2 937 319,77 | **+178 639,64** | ❌ (+6.5%) |
| Материальные | 2 635 997,55 | 2 823 192,87 | +187 195,32 | ❌ |
| Доп. расходы | 64 087,88 | 55 532,16 | -8 555,72 | ❌ |
| Постатейные постоянные | 58 594,71 | 58 594,71 | **0** | ✅ |

### Исправленные проблемы:
1. ✅ **Ошибка применения расширения** — убрана некорректная аннотация `&Вместо`
2. ✅ **Исправлена замена `&Числитель = 0`** — теперь `МАКСИМУМ(ВТПарыУзлов.Числитель) = 0`
3. ✅ **Добавлен `КоличествоПродукции = 1`** — для `ТекстДопРасходыОбъединение`
4. ✅ **Исправлено количество** — Антиокислитель 6PPD: 6,073 → 5,354 (было 152,95!)

### Оставшиеся проблемы:
1. ❌ **Отсутствуют 2 строки в новом отчёте**:
   - "Материальная себестоимость, таможенные пошлины_ТЗР" = 49 098,76
   - "Таможенные платежи" = 98,87
2. ❌ **Расхождение в Материальных** (+187 тыс.) — БОЛЬШЕ в новом (было МЕНЬШЕ в v179)
3. ❌ **Разница в количествах** — Антиокислитель 6PPD: 6,073 vs 5,354 (-11.8%)

### Анализ изменений v179 → v185:
- v179: Итого -5.2% (материальные МЕНЬШЕ)
- v185: Итого +6.5% (материальные БОЛЬШЕ)

Ключевое отличие: в v179 при `ЗнаменательУзла=0` вычислялся `ЗнаменательВычисленный = СуммаЧислителей`,
а в v185 используется оригинальный `ЗнаменательУзла=0` (автоматический расчёт SQL).

### Корневая проблема:
При пакетной обработке несколько узлов с ОДИНАКОВОЙ парой (Партия, Аналитика) но РАЗНЫМИ пропорциями
получают пропорцию ПЕРВОЙ группы. Это ведёт к неправильному распределению.

### Возможные решения:
1. **Добавить Числитель/Знаменатель в ключ JOIN** — SQL будет возвращать отдельные строки для каждой пропорции
2. **Использовать пост-распределение** — SQL возвращает 100%, а пост-обработка применяет индивидуальные коэффициенты
3. **Делать отдельные запросы для групп с разными пропорциями** — компромисс между точностью и производительностью

**Следующий шаг**: Реализовать вариант 1 — добавить поля Числитель/Знаменатель в структуру ВТПарыУзлов и изменить JOIN.