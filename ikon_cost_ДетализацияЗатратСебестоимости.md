# Регистр сведений: ikon_cost_ДетализацияЗатратСебестоимости

## Назначение

Кэширование результатов расчёта детализации затрат себестоимости продукции для ускорения формирования отчёта.

**Ключевые особенности:**
- Периодичность: месяц (дискретность расчёта)
- Режим записи: независимый (возможность ручной перезаписи)
- Инвалидация по версии алгоритма

---

## Свойства регистра

| Свойство | Значение |
|----------|----------|
| **Имя** | `ikon_cost_ДетализацияЗатратСебестоимости` |
| **Синоним** | Детализация затрат себестоимости (ikon_cost) |
| **Периодичность** | В пределах месяца |
| **Режим записи** | Независимый |
| **Набор записей** | Используется |

---

## Измерения (ключи записи)

| Имя | Тип | Описание | Индекс |
|-----|-----|----------|--------|
| `Организация` | `СправочникСсылка.Организации` | Организация | Да |
| `ПартияПродукции` | Определяемый тип `ДокументПартии` | Партия продукции (выходное изделие) | Да |
| `АналитикаУчетаПартийПродукции` | `СправочникСсылка.КлючиАналитикиУчетаПартий` | Аналитика учёта партий продукции | Да |
| `Уровень` | `Число(2, 0)` | Уровень вложенности (0 = продукция, 1+ = затраты/полуфабрикаты) | Да |
| `Номенклатура` | `СправочникСсылка.Номенклатура` | Номенклатура затраты | Да |
| `Характеристика` | `СправочникСсылка.ХарактеристикиНоменклатуры` | Характеристика затраты | Нет |
| `Серия` | `СправочникСсылка.СерииНоменклатуры` | Серия затраты | Нет |
| `ПартияЗатрата` | Определяемый тип `ДокументПартии` | Партия затраты (для полуфабрикатов) | Да |
| `АналитикаУчетаПартийЗатрата` | `СправочникСсылка.КлючиАналитикиУчетаПартий` | Аналитика учёта партий затраты | Да |
| `ТипЗатрат` | `Строка(255)` | Тип затрат (Материальные, Трудозатраты, Постатейные, Доп. расходы) | Нет |
| `Подразделение` | `СправочникСсылка.СтруктураПредприятия` | Подразделение | Нет |

---

## Ресурсы (суммовые поля)

| Имя | Тип | Описание |
|-----|-----|----------|
| `КоличествоЗатрата` | `Число(19, 6)` | Количество затраты |
| `СуммаЗатрата` | `Число(19, 2)` | Сумма затраты |
| `СуммаЗабалансовая` | `Число(19, 2)` | Забалансовая стоимость |

### Стоимости (основные)

| Имя | Тип | Описание |
|-----|-----|----------|
| `Материальные` | `Число(19, 2)` | Материальные затраты |
| `Трудозатраты` | `Число(19, 2)` | Трудозатраты |
| `ПостатейныеПостоянные` | `Число(19, 2)` | Постатейные постоянные |
| `ПостатейныеПеременные` | `Число(19, 2)` | Постатейные переменные |
| `ДопРасходы` | `Число(19, 2)` | Дополнительные расходы |
| `НДД` | `Число(19, 2)` | Налог на добавленный доход (отдельно от НДС) |

### Стоимости с НДС

| Имя | Тип | Описание |
|-----|-----|----------|
| `МатериальныеСНДС` | `Число(19, 2)` | Материальные с НДС |
| `ТрудозатратыСНДС` | `Число(19, 2)` | Трудозатраты с НДС |
| `ПостатейныеПостоянныеСНДС` | `Число(19, 2)` | Постатейные постоянные с НДС |
| `ПостатейныеПеременныеСНДС` | `Число(19, 2)` | Постатейные переменные с НДС |
| `ДопРасходыСНДС` | `Число(19, 2)` | Доп. расходы с НДС |

### Стоимости без НДС

| Имя | Тип | Описание |
|-----|-----|----------|
| `МатериальныеБезНДС` | `Число(19, 2)` | Материальные без НДС |
| `ПостатейныеПостоянныеБезНДС` | `Число(19, 2)` | Постатейные постоянные без НДС |
| `ПостатейныеПеременныеБезНДС` | `Число(19, 2)` | Постатейные переменные без НДС |
| `ДопРасходыБезНДС` | `Число(19, 2)` | Доп. расходы без НДС |

### Стоимости управленческие

| Имя | Тип | Описание |
|-----|-----|----------|
| `МатериальныеУпр` | `Число(19, 2)` | Материальные управленческие |
| `ТрудозатратыУпр` | `Число(19, 2)` | Трудозатраты управленческие |
| `ПостатейныеПостоянныеУпр` | `Число(19, 2)` | Постатейные постоянные упр. |
| `ПостатейныеПеременныеУпр` | `Число(19, 2)` | Постатейные переменные упр. |
| `ДопРасходыУпр` | `Число(19, 2)` | Доп. расходы управленческие |
| `СтоимостьЗабалансоваяУпр` | `Число(19, 2)` | Забалансовая стоимость упр. |

### Стоимости регламентированные

| Имя | Тип | Описание |
|-----|-----|----------|
| `МатериальныеРегл` | `Число(19, 2)` | Материальные регламентированные |
| `ТрудозатратыРегл` | `Число(19, 2)` | Трудозатраты регламентированные |
| `ПостатейныеПостоянныеРегл` | `Число(19, 2)` | Постатейные постоянные регл. |
| `ПостатейныеПеременныеРегл` | `Число(19, 2)` | Постатейные переменные регл. |
| `ДопРасходыРегл` | `Число(19, 2)` | Доп. расходы регламентированные |
| `СтоимостьЗабалансоваяРегл` | `Число(19, 2)` | Забалансовая стоимость регл. |

### Налоговый учёт

| Имя | Тип | Описание |
|-----|-----|----------|
| `НалоговыйУчет` | `Число(19, 2)` | Налоговый учёт |
| `ПостояннаяРазница` | `Число(19, 2)` | Постоянная разница |
| `ВременнаяРазница` | `Число(19, 2)` | Временная разница |

---

## Реквизиты (служебные)

| Имя | Тип | Описание |
|-----|-----|----------|
| `ВерсияРасчёта` | `Строка(20)` | Версия алгоритма расчёта (для инвалидации кэша) |
| `ДатаРасчёта` | `Дата` | Дата и время расчёта |
| `ТребуетсяРазузлование` | `Булево` | Флаг для отложенного разузлования |
| `ИдентификаторСтроки` | `УникальныйИдентификатор` | Уникальный идентификатор строки |

---

## Индексы

### Индекс 1 (Основной)

**Имя:** `Индекс1`

**Поля:**
1. `Период` (убывание)
2. `Организация`
3. `ПартияПродукции`
4. `АналитикаУчетаПартийПродукции`

**Назначение:** Быстрый поиск записей по периоду, организации и партии продукции.

### Индекс 2 (По затрате)

**Имя:** `Индекс2`

**Поля:**
1. `Период` (убывание)
2. `ПартияЗатрата`
3. `АналитикаУчетаПартийЗатрата`

**Назначение:** Поиск «детей» при разузловании (какие партии используют данный полуфабрикат).

### Индекс 3 (По номенклатуре)

**Имя:** `Индекс3`

**Поля:**
1. `Период` (убывание)
2. `Номенклатура`

**Назначение:** Отбор по продукции в отчёте.

---

## Алгоритм использования

### При формировании отчёта

```
1. Проверка наличия данных в регистре за период
2. Если данных нет или версия не совпадает:
   - Выполнить полный расчёт через ikon_cost_ПостроитьДеревоСебестоимости
   - Записать результат в регистр
3. Получить данные из регистра с применением отборов
```

### Инвалидация кэша

Кэш инвалидируется при:
- Изменении версии алгоритма (`ВерсияРасчёта`)
- Ручном пересчёте за период
- Изменении данных за период (документы расчёта себестоимости)

### Регламентное обновление

Рекомендуется настроить регламентное задание:
- **Периодичность:** ежедневно / еженедельно
- **Время:** ночное (минимальная нагрузка)
- **Действие:** пересчёт за предыдущий месяц / текущий месяц

---

## XML для создания в конфигураторе

```xml
<Metadata name="InformationRegister.ikon_cost_ДетализацияЗатратСебестоимости" 
          id="СГЕНЕРИРОВАТЬ-GUID" 
          configVersion="...">
    <Properties>
        <Name>ikon_cost_ДетализацияЗатратСебестоимости</Name>
        <Synonym>
            <v8:item>
                <v8:lang>ru</v8:lang>
                <v8:content>Детализация затрат себестоимости (ikon_cost)</v8:content>
            </v8:item>
        </Synonym>
        <UseStandardCommands>true</UseStandardCommands>
        <EditType>InDialog</EditType>
        <DefaultRecordForm/>
        <DefaultListForm/>
        <RecordSetPresentation/>
        <RecordPresentation/>
        <ExtendedRecordPresentation/>
        <ExtendedListPresentation/>
        <ListPresentation/>
        <Position>Auto</Position>
        <RecordSetPresentationField/>
        <MainFilterOnPeriod>true</MainFilterOnPeriod>
        <Periodicity>Month</Periodicity>
        <DataLockControlMode>Managed</DataLockControlMode>
        <WriteMode>Independent</WriteMode>
    </Properties>
    <!-- Измерения, ресурсы, реквизиты, индексы -->
</Metadata>
```

---

## Преимущества

| Аспект | До | После |
|--------|-----|-------|
| **Время отчёта (повторный запуск)** | Минуты/часы | Секунды |
| **Нагрузка на сервер** | Каждый раз полная | Только при обновлении кэша |
| **Детализация** | Вычисляется каждый раз | Берётся из регистра |
| **Инвалидация** | — | По версии алгоритма или периоду |
| **Гибкость отбора** | Полная | Полная (отборы применяются к регистру) |
